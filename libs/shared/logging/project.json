{
  "name": "@whizard/shared-logging",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "root": "libs/shared/logging",
  "sourceRoot": "libs/shared/logging/src",
  "projectType": "library",
  "tags": ["type:shared", "scope:logging"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsc -p libs/shared/logging/tsconfig.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}

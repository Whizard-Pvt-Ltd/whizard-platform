import { describe, it } from 'vitest';

describe('PrismaUserAccountRepository', () => {
  it.todo('upserts user account root data without deleting history tables');
  it.todo('finds account by id and by primary email');
});

import { describe, it } from 'vitest';

describe('PrismaUserSessionRepository', () => {
  it.todo('upserts session root and preserves issued timestamps');
  it.todo('returns active sessions ordered by issuedAt descending');
});

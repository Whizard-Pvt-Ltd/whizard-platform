import { describe, it } from 'vitest';

describe('PrismaCredentialRepository', () => {
  it.todo('stores credential rows for a user account with historical records preserved');
  it.todo('returns active credential by user and credential type');
});

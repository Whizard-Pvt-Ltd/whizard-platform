import { describe, it } from 'vitest';

describe('PrismaAccessPrincipalRepository', () => {
  it.todo('persists principal root and role/permission/restriction child rows atomically');
  it.todo('rebuilds the aggregate from principal plus child rows');
});
